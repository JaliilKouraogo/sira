/**
 * Le référentiel des énumérations vit dans le paquet partagé `@sira/shared`,
 * lu à la fois par le site et par l'API. Ce fichier le réexporte pour que les
 * écrans continuent d'importer depuis `@/lib/enums`.
 */
export * from "@sira/shared";
